# RealEstate_website

This repository uses a two-level layout:

- Root (`./`) is a thin wrapper with helper scripts.
- App source lives in `./project`.

Use root commands for convenience:

- `npm run dev`
- `npm run build`
- `npm run preview`

These commands delegate to `./project` and keep the current setup working
without requiring you to `cd project` first.